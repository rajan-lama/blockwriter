import type { MultiOriginPalettes, GlobalStylesConfig } from '../types';
/**
 * Retrieves color and gradient related settings.
 *
 * The arrays for colors and gradients are made up of color palettes from each
 * origin i.e. "Core", "Theme", and "User".
 *
 * @param settings Global styles settings.
 *
 * @return Color and gradient related settings.
 */
export default function getPalettes(settings?: GlobalStylesConfig): MultiOriginPalettes;
//# sourceMappingURL=get-palette.d.ts.map