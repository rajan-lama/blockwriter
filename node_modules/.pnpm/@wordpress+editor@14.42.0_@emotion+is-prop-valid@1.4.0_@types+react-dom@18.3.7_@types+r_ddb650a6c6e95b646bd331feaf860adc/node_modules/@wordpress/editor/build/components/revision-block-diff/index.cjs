"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// packages/editor/src/components/revision-block-diff/index.js
var revision_block_diff_exports = {};
__export(revision_block_diff_exports, {
  default: () => RevisionBlockDiffPanel
});
module.exports = __toCommonJS(revision_block_diff_exports);
var import_components = require("@wordpress/components");
var import_block_editor = require("@wordpress/block-editor");
var import_data = require("@wordpress/data");
var import_i18n = require("@wordpress/i18n");
var import_post_panel_row = __toESM(require("../post-panel-row/index.cjs"));
var import_jsx_runtime = require("react/jsx-runtime");
function RevisionBlockDiffPanel() {
  const { block } = (0, import_data.useSelect)((select) => {
    const { getSelectedBlock } = select(import_block_editor.store);
    return {
      block: getSelectedBlock()
    };
  }, []);
  if (!block) {
    return null;
  }
  const diffInfo = block.attributes?.__revisionDiffStatus;
  const changedAttributes = diffInfo?.changedAttributes;
  if (!changedAttributes) {
    return null;
  }
  const fields = Object.entries(changedAttributes).map(
    ([key, parts]) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_post_panel_row.default, { label: key, children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { className: "editor-revision-fields-diff__value", children: parts.map((part, index) => {
      if (part.added) {
        return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(
          "ins",
          {
            className: "editor-revision-fields-diff__added",
            children: part.value
          },
          index
        );
      }
      if (part.removed) {
        return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(
          "del",
          {
            className: "editor-revision-fields-diff__removed",
            children: part.value
          },
          index
        );
      }
      return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { children: part.value }, index);
    }) }) }, key)
  );
  return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_components.PanelBody, { title: (0, import_i18n.__)("Changed attributes"), initialOpen: true, children: fields });
}
//# sourceMappingURL=index.cjs.map
